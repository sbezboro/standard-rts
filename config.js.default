var config = {
  debug: true,
  website: '127.0.0.1',
  port: 3000,

  authSecret: 'secret',

  mcApiPort: 20059,
  mcApiUsername: 'username',
  mcApiPassword: 'password',
  mcApiSalt: 'salt'
}

module.exports = config;
